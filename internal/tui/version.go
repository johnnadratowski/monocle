package tui

// Version is the client (TUI) build version, injected from main at startup. Shown
// in the title bar next to the app name, and compared against the connected
// engine's version to flag a mismatch.
var Version = "dev"
